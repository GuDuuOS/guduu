# CosMac Star 服务运行依赖（部署到服务器/Google Cloud 时用：pip install -r guduu/requirements.txt）
# 注意：这是 CosMac Star 自己的 AI 服务依赖，不含 Synapse 本体。
requests>=2.31
anthropic>=0.69
openai>=1.50
# 数据层（模块2 起）：SQLAlchemy 同步 ORM。生产连 PostgreSQL，本地默认回退 SQLite。
SQLAlchemy>=2.0
# 生产连 Postgres 用（本地走 SQLite 时不需要）。装不上不影响本地开发/测试。
psycopg[binary]>=3.1; platform_system != "Windows"
# 知识库向量检索（pgvector）后续接入时再启用：
# pgvector>=0.2
